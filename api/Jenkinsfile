pipeline {
    agent any
    tools {
        maven 'Maven'
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn api/create-deploy/pom.xml clean install -B -DskipTests'
            }
        }
    }
}