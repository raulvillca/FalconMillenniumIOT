pipeline {
    agent any
    tools {
        maven 'Maven'
    }
    stages {
        stage('Build') {
            steps {
                sh 'cd api & cd create-deploy'
                sh 'mvn -B -DskipTests clean install'
            }
        }
    }
}